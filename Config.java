package util;

public class Config {
    public static boolean ENABLE_TIMER = true;    // enable per-level timer
    public static double LEVEL_TIME_SECONDS = 90; // seconds per level
    public static int WORLD_MULTIPLIER = 2;       // world = multiplier * screen in each axis
    public static boolean WRAP_WORLD = true; // wrap entities at world edge
}
